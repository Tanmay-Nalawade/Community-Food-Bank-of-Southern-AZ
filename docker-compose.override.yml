# Auto-loaded by `docker compose up` for local development.
# Mounts your code and runs nodemon so edits restart the server.
services:
  app:
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev
    environment:
      NODE_ENV: development
