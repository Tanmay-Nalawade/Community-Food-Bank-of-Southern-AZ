services:
  app:
    build: .
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    env_file:
      - .env
    environment:
      PORT: ${PORT:-8080}
      MONGO_DB_URL: ${MONGO_DB_URL}
    restart: unless-stopped
